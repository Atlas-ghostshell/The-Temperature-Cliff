# The-Temperature-Cliff
A Week 3 AI security lab that set out to test LLM sampling behavior — and instead caught the exact model-generation boundary where Anthropic began hard-deprecating the temperature parameter in favor of reasoning-effort controls, with real divergence data from the last generation where it still works.
